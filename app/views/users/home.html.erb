<% provide(:maintitle, "#{current_user.name}'s Home") %>
<% provide(:title, "#{current_user.name}'s Home") %>
<% if @feed_items.any? %>
  <ol class="feed_items">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
